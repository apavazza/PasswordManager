name: Build and Create Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-linux:
    name: Build - Linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2
        with:
          submodules: true

      - name: Build
        run: make

      - name: Rename
        run: mv bin/PasswordManager PasswordManager_linux_x64_${{ github.ref_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4.6.2
        with:
          name: PasswordManager
          path: ./PasswordManager_linux_x64_*
          retention-days: 1

  create_release:
    name: Create release
    runs-on: ubuntu-latest
    needs: [build-linux]

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4.2.1
        with:
          name: PasswordManager

      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1.13.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          name: Release ${{ github.ref_name }}
          tag: ${{ github.ref_name }}
          draft: true
          prerelease: false
          artifacts: PasswordManager_*
